---
description: 阿泰小店项目约定与重要配置
alwaysApply: true
---

# 阿泰小店项目约定

## ⚠️ 强制：改完代码必须立即提交部署（最高优先级）

**每次修改任何代码后，在回复用户之前，必须完成：**
1. `git add` 修改的文件
2. `git commit -m "描述"`
3. `git push`

**禁止**等用户说「提交」「部署」「推送」才执行。改完代码 → 立刻提交推送 → 再回复用户。

---

## ⚠️ 强制：修 Bug / 改业务代码前（用户明确要求）

**用户约定：出现线上或本地异常时，助手在修订代码前，必须先向用户索要证据与日志，再动手；禁止一上来就凭猜测改一大片。**

1. **先索要（至少择一，能多则多）**  
   - **Vercel → Logs**：含 **`at ... (` 堆栈** 的几行，或 **`[NOTION_SITE_DATA]`** 整行 JSON；  
   - **浏览器 F12**：Console 红色报错全文；Network 里失败请求的 **状态码 + Request URL**；  
   - **复现步骤**：哪个 URL、必现还是偶发。  

2. **再改代码**  
   在拿到证据并**对齐堆栈/错误信息**后再修改；说明「改什么、对应日志里哪一条」。  

3. **禁止**  
   在**没有任何**运行时日志、堆栈或用户已贴出的明确报错行的情况下，**主动**连续多处「试探性」修改业务/Notion/缓存逻辑。  

4. **例外（可不经由完整日志）**  
   - 用户**明示**「不用日志、直接按我说的改」；  
   - **单点**修改且用户已给出**完整**报错位置与期望行为；  
   - 纯文案、注释、明显笔误。  

5. **预期**  
   无人能承诺「百分百一次修好」，但**证据优先**可最大限度避免瞎改；若证据不足，应先说明还缺什么，而不是先改代码。

---

## 项目性质
- **私人商品展示网站**，不对外公开推广
- **禁止搜索引擎收录**：ROBOTS_ALLOW 必须保持 false，切勿改为 true

## 技术栈
- NotionNext + heo 主题
- 部署在 Vercel
- 商品数据来自 Notion 数据库
- **Notion 仅自己看的备注**：在数据库增加列（默认列名 **`内部备注`**，与 `conf/notion.config.js` 的 **`NOTION_PRIVATE_PROPERTY_NAMES`** 一致）；构建时在 **`getPageProperties`** 末尾从 post 对象**删除**这些键，**不进入网站**。多列用英文逗号；可设环境变量 **`NOTION_PRIVATE_PROPERTY_NAMES`**。机密勿写标题/正文/slug；持有 **Notion Token** 的服务器仍会拉取 API，访客端不展示。
- **商品描述（原 summary）列名**：`NOTION_PROPERTY_NAME.summary` 默认 **`商品描述`**（Notion 显示）；仍兼容 **`summary`** / **`摘要`** / **`描述`**，改名不丢内容。
- **价格 / 标签 / 分类 / 链接列名**：默认 **`价格`**、**`标签`**、**`分类`**、**`自定义链接`**（网址路径，对应程序字段 `slug`）；仍兼容 **`price`**、**`tags`**、**`category`**、**`slug`**、**`链接别名`**。

### 搜索与全屏加载
- **入口**：**`themes/heo/components/SearchInput.js`**：用 **`window.location.assign`** 打开 **`/search/关键词`**（**`encodeURIComponent`**），必要时带 **`theme`**；**不用** **`router.push`**，避免客户端等数据时全屏加载卡死。空关键词则 **`assign('/')`**。
- **`pages/search/index.js`**：**`getStaticProps`**；无关键词时展示搜索导航；有 **`?s=`** 时仅在浏览器对 **标题、商品描述 summary、标签、分类** 做过滤（**不读 Notion 正文**）。
- **`pages/search/[keyword]`** 与 **`pages/search/[keyword]/page/[page]`**：**`getStaticProps`** + **`filterPostsByKeyword`**；**仅**匹配 **标题、商品描述 summary、标签、分类**（**不**检索 Notion 正文，避免搜索卡死/超时）。**`next build`** 的 **`NEXT_SEARCH_NO_REMOTE=1`** 等仍保留用于历史兼容与其它路径，与搜索正文无关。
- **`lib/global.js`**：**`/search` 与 `/search/*` 路径**不打开全屏 LoadingCover；其它路由仍 **25s** 兜底 **`setOnLoading(false)`**；**勿**把 **`onLoading`** 放进 **`routeChangeComplete` 监听**的 effect 依赖。
- **`LayoutSearch`（heo）**：`currentSearch` 须含 **`router.query.keyword`**（`/search/[keyword]` 动态段），不能只用 **`props.keyword`**；否则在 **`fallback: true`** 首屏 props 未就绪时会误判「无关键词」→ 只见空白的分类/标签导航。与 **`router.query.s` / `q`**（`/search` 页）一并兼容。**`/search/[keyword]`** 建议使用 **`fallback: 'blocking'`**，首访等数据就绪再出页。

### 搜索页布局（`/search` 有搜索词时）
- 顺序：**「搜索结果」** + 商品列表 → **「按标签筛选」** → 底部分类/标签；PC 与手机一致（`themes/heo/index.js` LayoutSearch）
- **`filterPostsByKeyword`**（`lib/search/filterPostsByKeyword.js`）：**仅元数据**子串匹配，**已不做正文检索**。重要词请写在 **标题或商品描述** 等可在列表里拿到的字段。另：全站 `post` 会 **`delete post.content`**，详情页正文渲染须从 **blockMap 根块** 回退子块 id（与搜索无关）。

### 「所有商品」列表排序
- `blog.config.js` 在合并 `post.config` 后 **强制 `POSTS_SORT_BY: 'date'`**（post.config 默认是 `notion`，会不按时间排）
- 排序字段为每条商品的 **`publishDate`**：优先取 Notion **日期（date）** 属性，没有则用页面 **创建时间**
- 若 Notion 配置表里有 `POSTS_SORT_BY` 且改成非 `date`，会覆盖上述行为

### 首页 Hero 横滑商品
- **手机端**：标题 **「热销款」**（仅 `xl` 以下，无额外提示文案）；热销款区为 **小图两排竖叠 × 多列横滑**。**桌面端（`xl+`）** 热销款为三列竖向滚轮网格等（见 `Hero.js`）。**曾出现问题**：`BannerGroup` 在移动端子组件虽 `hidden` 但外层仍 `flex-1` 占宽 → 左侧大片空白、热销条挤到右侧；已改为 **`BannerGroup` 在 `xl` 以下整块 `hidden`**，且中间留白仅 `xl` 显示。
- **置顶逻辑**：Notion 数字列 **「首页置顶」**（列名可 `NEXT_PUBLIC_NOTION_PROPERTY_HERO_PIN`）；`1` 最左、`2` 其次…；`0`/空=不参与
- 优先级：① Notion 置顶 ② `NEXT_PUBLIC_HERO_PINNED_SLUGS` ③ 推荐标签 ④ 最近更新
- **条数**：`themes/heo/config.js` 的 `HEO_HERO_TOP_MAX` 或 `NEXT_PUBLIC_HEO_HERO_TOP_MAX`，默认 **80**，最大 **80**
- **生效时间**：页面 ISR 默认约 **12 小时**（`NEXT_REVALIDATE_SECOND`，可改 env）；改价后一般**自动**跟上，无需配密码。若要**立刻**见可 **Vercel Redeploy**。

### Vercel 构建失败「429 Too Many Requests」
- **成因**：`next build` 时多 worker 并行请求 Notion，易触发官方限流；日志里常见 `[API<<--异常]: HTTPError: 429`、`slug-props-article`，以及 `Jest worker ... child process exceptions`（多为子进程里 API 异常退出，不一定是业务代码写错）。
- **工程缓解**：`package.json` 的 **`build`** 设 **`NOTION_BUILD_THROTTLE=1`**（`cross-env`）；`lib/notion/getNotionAPI.js` 包装 **`notion.fetch`** + **`getPage` 强制 `concurrency: 1`**；对 **got** 注入 `hooks` 串行 HTTP + 间隔（`NOTION_HTTP_GAP_MS`）。**`lib/notion/RateLimiter.ts`** 跨进程文件锁：**勿**在构建场景用 30s「陈旧锁」删除——等待中的 worker 会误删仍在执行超长 `getPage` 的锁，导致多 worker 同时打 Notion → 429；构建/CI 下陈旧阈值改为 **1 小时**。另：**429 时 got 先 `afterResponse` 再 `beforeError`**，须 **幂等释放** HTTP 槽位。
- **临时**：短时间连续多次 Redeploy 仍可能 429，可隔几分钟再部署一次。
- **构建耗时**：限流生效后单次部署 **5～10+ 分钟** 属正常（307 页量级 + 每条 Notion HTTP 约 900ms 间隔 + 全局串行）；要速度需承担再次 429 风险，一般不建议动。
- **构建黄条**：`engines` 已固定 **`20.x`** 与 Vercel 一致；`react-notion-x` 的 yarn peer 警告可忽略；`data for page ... exceeds 128 kB` 来自 Next 对 **getStaticProps 体积**提示，Notion 全站数据进 props 时常见，非部署失败，长期可做数据瘦身（另议）。

### Vercel 构建失败 prerender：`Cannot read properties of undefined (reading 'replace')`
- **成因**：`react-notion-x` 渲染块时用 `notion-utils` 的 `uuidToId(block.id)`；部分 block 的 **`value.id` 缺失**（如同步块展开后子块未带 id）会在 SSR 预渲染时崩溃；与「点了几次 Redeploy」无直接关系。
- **缓解**：`lib/notion/ensureRecordMapBlockIds.js` 在 `getPostBlocks` 转换后及 `NotionPage` 渲染前用 **recordMap 的 key 补齐 `value.id`**；`mapPageUrl` 对空 id 做兜底。

### 商品详情正文/图片整段空白（首页列表却有图）
- **成因**：`react-notion-x` 的 `NotionBlockRenderer` 未传 **`blockId`** 时默认取 **`Object.keys(recordMap.block)[0]`**，而 Notion 返回的 block map **键顺序不保证**，第一个键往往不是当前页的 **`type===page`** 根块 → 子内容（含图片）不挂到正确树上。
- **成因 2（6.x）**：`NotionBlockRenderer` 直接读 **`recordMap.block[id].value`**；若接口返回 **`value` 套一层**，不展开则 **无 `.type` / `.content`**，整页空白。
- **修复**：`unwrapRecordMapBlockValueLayers` 仅当 **外层无 `type`、内层 `value` 有 `type`** 时展开（**勿**用 `!properties` 判断：合法 `collection_view_page` 的 `properties` 可为 `undefined`，误展开会破坏根数据库块 → 全站 **`无法获取 Notion 数据`** 占位）。
- **其它**：`NotionPage` 传 **`blockId` / `rootPageId`**；**`signed_urls`** 互备 + **`mirrorSignedUrlsForBlockIds`**。

### 全站只剩「无法获取 Notion 数据」占位商品（slug=oops）
- **常见原因**：Vercel **Production** 未配置 **`NOTION_PAGE_ID`** / **`NOTION_ACCESS_TOKEN`**，或值含**首尾空格/换行**；或 **`site_data_*` 缓存**中仍为旧错误结果。
- **误判数据库**：占位文案里已显示 **当前 `xxxxxxxx…`（32 位 hex）** 时，说明 **ID 已传入**，多为 **`recordMap.block[id].value` 套娃**（外层无 `type`），`convertNotionToSiteData` 直接读 `.value` 得不到 **`collection_view_page` / `collection_view`** → 误判非数据库 → `EmptyData`。已在 **`getRecordMapEntryBlockValue`** 中按层展开后再判型。
- **仍占位**：再补 **`findRecordMapBlockEntryForPageId`**（block key 与 pageId 横线/大小写不一致）；**只要有 `collection_id`** 即视为数据库；全表扫描任一块带 **`collection_id`** 的块作 metadata；子块兼容 **`collection_view_page`**。站点缓存键 **`site_data_v4_`**。
- **Redis 占位毒化**：构建期会 **`ENABLE_CACHE=true`** 并读 Redis；若曾把 **`slug=oops` 占位**写入 **`site_data_*`**，后续构建会一直命中缓存、**不再请求 Notion**。**`lib/cache/cache_manager.js`**：`site_data_*` **不把占位当有效缓存**（读写均跳过）；**`getDataFromCache`** 与 **`setDataToCache`** 一样在 **Vercel 生产运行时**不读写（避免「只读 Redis、运行期不写」长期吃脏数据）；搜索等路径可用 **`force`** 仍读缓存。
- **工程侧**：`blog.config.js` **`trim`**；**`next build`** 无 `NOTION_PAGE_ID` 时抛错。
- **可观测性（定位不靠猜）**：失败路径会打 **`[NOTION_SITE_DATA]`** + JSON（含 **`reason`**：`NOTION_PAGE_ID_EMPTY` / `NO_PAGE_RECORDMAP` / `NOT_DATABASE` / `EMPTY_ARTICLE_LIST` / `GET_GLOBAL_DATA_THROW` 等）。在 Vercel → Logs 搜该前缀。可选：环境变量 **`SITE_DIAG_SECRET`**（≥8 字符）后 **`GET /api/health-site`**，带 **`Authorization: Bearer <secret>`**，返回是否占位、列表条数、ID/Token 是否已配置（不泄露密钥）。
- **排障顺序（助手侧）**：与上文 **「修 Bug 前先索要证据」** 一致；线上异常**优先**向用户索要 **Vercel → Logs** 堆栈或 **`[NOTION_SITE_DATA]`** 整行，**有证据再改代码**；无运行时日志时只可说明「可能原因」与「待验证」，**不得**把推测说成已定位根因。
- **`GET_GLOBAL_DATA_THROW` + `ids is not defined`**：曾由 **`lib/notion/getAllPageIds.js`** 的 `catch` 误引用 **`ids`**（该变量仅在内部 `if` 块内声明）导致 **`ReferenceError`**，全站 **`getGlobalData`** 失败。已改为 **`catch` 只打 `error`**，并对 **`collectionQuery[collectionId]`** 做存在性判断与 **`queryForCollection`** 回退。**无需 REDIS** 也会触发。

### Vercel 构建：`Conflicting paths ... /[prefix]/[slug]` 与 `/[prefix]/[slug]/[...suffix]`
- **成因**：Notion 里 slug 若以 **`/article/xxx`** 形式开头（前导 `/`），`getStaticPaths` 若直接 **`row.slug.split('/')`** 会得到空串段，与去掉 `/` 后的 **`article/xxx`** 生成**同一路径**，Next 报冲突。
- **修复**：`lib/utils/post.js` **`normalizeSlugForPath`**；`pages/[prefix]/[slug]/index.js` 与 **`[...suffix].js`** 的 `getStaticPaths` 均用规范化后再分段；多级路由 **`suffix` 为空则不输出**。无缓存 Redeploy 会完整跑静态路径，更易触发；非「手动部署才坏」，是**无缓存时必现**类问题。

### Vercel 部署 `yarn install` 失败：`mirrors.cloud.tencent.com` 525
- **成因**：`yarn.lock` 里若 **`resolved` 锁成腾讯云 npm 镜像**，海外构建机访问常 **HTTP 525**。
- **约定**：`.npmrc` 使用 **`registry=https://registry.npmjs.org/`**；勿把 **tencent 镜像 URL** 提交进 `yarn.lock`（本地若用国内源装依赖，装完应用官方源重写 lock 再提交）。

## 依赖漏洞
- GitHub 提示的 19 个漏洞**暂时可忽略**，此前已处理过一批，剩余的可不处理
- 不要建议用户去修复这些漏洞，除非用户主动提出

## 规则管理
- 可自由添加或修改项目规则，无需事先征得同意
- 每次添加或修改规则后，**必须立即告知用户**具体改了什么

### ⚠️ 主动沉淀：重要对话必须写入规则
**禁止等用户提醒。** 当对话中出现以下任一情况时，**必须主动**更新 `project-context.mdc`：
- 用户明确的选择或偏好（如「不备案」「不用 CDN」）
- 重要配置的说明或约定（如某功能在哪查看、如何开启）
- 反复确认过的设定（如图片/浏览器缓存 30 天、面向国内用户）
- 用户纠正过的错误（如「改完要提交部署」）

**流程**：发现重要内容 → 立刻写入规则 → 提交推送 → 再回复用户。

---

## 性能与缓存（面向国内用户，客户无 VPN）

### 页面 ISR 与图片缓存（分开理解）
- **NEXT_REVALIDATE_SECOND**：默认 **43200（12 小时）**— 访客访问后超过该时间，后台用 Notion 数据重新生成该页（价格、标题等）。**无需**任何密钥或额外配置。仍要 **30 天** 可在 Vercel 设 **`NEXT_REVALIDATE_SECOND=2592000`**；若 Notion 站点配置表里有 **`NEXT_REVALIDATE_SECOND`** 也会覆盖 `blog.config.js` 默认值。
- **LIST_REVALIDATE_SECOND**（可选，Vercel 环境变量）：**首页**与**商品列表分页**（`pages/index.js`、`pages/page/[page].js`）的 ISR 间隔为 **`min(NEXT_REVALIDATE_SECOND, LIST_REVALIDATE_SECOND)`**，默认 **900（15 分钟）**。新上架商品在间隔过后由**下一次访问触发再生成**即可出现在「所有商品」，一般不必整站 Redeploy。要更快可设 `300` 等；若设成 **≥** 当前全站 `NEXT_REVALIDATE_SECOND`，则列表页与全站一致（不再单独缩短）。
- **首页列表图宽度（仅加速、不降详情清晰度）**：`themes/heo/config.js` 中 **`HOME_LIST_COVER_MAX_WIDTH`**（默认 **480**，可改；曾 680）、**`HOME_HERO_THUMB_MAX_WIDTH`**（热销款小卡封面，默认 **200**；曾 560/320）、**`HOME_HERO_ICON_MAX_WIDTH`** / **`HOME_HERO_CARD_MAX_WIDTH`** 仅作用于首页 Hero、首页与 `/page/N` 商品卡封面；**商品详情头图与正文图**仍走 **`IMAGE_COMPRESS_WIDTH`（默认 900）**，勿混用。**手机端列表封面**可另设 **`HOME_LIST_MOBILE_PROXY_QUALITY`**（默认 **18**，`0` 关闭）：通过 `/api/proxy-image?q=` + sharp 转 WebP，略降清晰度、减小体积；PC 仍用原代理 URL（无 `q`）。列表网格手机端 **`gap-2`**、`BlogPostCard` **`mb-2`** / 文案区 **`p-2`**，桌面端保持原间距。
- **proxy-image**：图片代理仍 **30 天**（`pages/api/proxy-image.js`），与页面 ISR **无关**，不因改 12 小时而缩短。
- **BROWSER_CACHE_TTL / CDN_CACHE_TTL**：仍为 **2592000**（30 天）除非另改配置。

### 图片代理（必须开启）
- **IMAGE_PROXY_ENABLE** 必须为 true，默认开启
- 国内用户直连 Notion 慢/可能被墙，图片经本站 `/api/proxy-image` 中转，无需 VPN
- **同步块展开**：`getPostBlocks` 里子块优先用 **`value.id` 作 recordMap 的 key**（与 `signed_urls` 一致）；`NotionPage` 对 **`blockMap` 先深拷贝再补 id**，避免就地改 props 引用导致异常。`mapImgUrl` 在缺 `block.id` 时不拼 `id=undefined`。

### 图片压缩
- **IMAGE_COMPRESS_WIDTH: 900**、**IMG_COMPRESS_WIDTH: 900**
- **IMAGE_COMPRESS_QUALITY: 80**

### next.config.js
- `formats: ['image/avif', 'image/webp']` — 优先现代格式
- `poweredByHeader: false`

### 监控
- **Vercel Analytics**：默认开启（`ANALYTICS_VERCEL`），访问量统计
- **不蒜子（ANALYTICS_BUSUANZI）**：**已全局关闭**（`lib/config.js` 对 `ANALYTICS_BUSUANZI_ENABLE` 固定 `false`），不再请求外网统计脚本，避免国内超时拖慢；heo 侧栏统计卡仅保留 **商品数量 / 建站天数**。
- **Speed Insights**：已集成，加载速度（LCP、FID、CLS）在 Vercel 控制台查看
- **商品详情页访问量**：已移除（真实统计需注册 Redis 服务，用户不想要）

### SmartLink
- **weixin://、mailto:、/wechat-qr.png 等**必须用原生 `<a>`，不能用 Next `Link`；否则客户端路由会误判为站内页面 → **404**（移动端三条杠里「微信联系」曾因此出错）

### 联系方式
- **PC 侧栏**：电报 + 微信紧贴并排（TouchMeCard + WeChatCard），下方 ContactButtons 展示 WhatsApp、QQ、邮件
- **邮件图标**：页脚 SocialButton、移动端三条杠菜单 SlideOver 均展示邮件联系，与首页一致
- **微信**（用户已配置，需牢记）：
  - **跳转链接**（手机端点击直接打开微信）：Vercel 环境变量 `NEXT_PUBLIC_CONTACT_WECHAT` = `weixin://dl/chat?username=微信号`（永久有效，把「微信号」换成真实微信号）
  - **二维码图片**：① 图片放 `public/wechat-qr.png`；② Vercel 环境变量 `NEXT_PUBLIC_CONTACT_WECHAT_QR` = `/wechat-qr.png`；③ 可同时配置跳转链接，弹窗会显示「点击打开微信」按钮 + 二维码
  - **行为**：手机端点链接可跳转微信，电脑端无微信时需扫码；同时配置链接+二维码时体验最佳

### 回到顶部
- **BackToTopButton**：仅滚动到底部（距底 80px 内）时显示，主内容区右下角，不遮挡侧边栏

### 加载过渡
- **LoadingCover**：柔和马卡龙配色二次元风格，银灰粉发少女 + 白色小团子 + 漂浮爱心/光点 + 进度条，支持深色模式

### 关于页面
- **静态备用**：`pages/about.js` + heo 主题 `LayoutAbout`，保证 `/about` 始终可访问（含年龄限制警告）
- 若 Notion 中有 slug=`about` 的页面，会走 `[prefix]` 动态路由；静态页优先级更高，当前使用静态页

### 手机端专项
- **按标签筛选**（TagFilterBar）：手机端固定 **2 行** + 横向滑动；桌面换行网格
- 首页 **Hero 热销款** 与主内容区 **商品分类**（`HomeCategoryGrid`）版式对齐：手机**同一断点**（`xl`）下均为 **仅区块标题** + **两排小卡、列宽同 TopGroup、横滑**；`xl+` 均为**多列大卡片**（商品分类为更多列的网格、桌面卡上显示件数；顶栏 `CategoryBar` 仍为纯文字+箭头快速跳转，不是图片小卡）。**顶栏手机**见 `Header.js`：`lg:hidden` 单独 `flex` 行，左 `flex-1 min-w-0 basis-0 overflow-hidden`、右 `shrink-0`；桌面 `hidden lg:flex` 另一套；**`SearchButton` 外包 `div`**（其根为 Fragment）。
- 回到顶部：手机端阈值 150px，增加 resize 监听和延迟检查
- 搜索图标：手机端 48×48px、图标 text-xl
- **三条杠菜单**（SlideOver）：「关于」上方显示网站名称（`siteConfig('TITLE')`），与首页顶部 Logo 一致，不再显示「博客」

### 不做
- **CDN 加速**：用户选择不备案，国内 CDN 需备案，暂不接入
- **备案**：Vercel + 国际域名（.com）无需备案，可长期正常使用

---

## 给之后对话的说明（勿删）
- **持久记忆**：以本仓库 **`.cursor/rules/project-context.mdc`** 为准（`alwaysApply: true`），新会话会先加载；重要约定已写在上文。
- **用户习惯**：回复用 **简体中文**；阿泰小店 = 私人商品站、Vercel、heo 主题、Notion 数据源。
- **勿让用户手工点运行**：改代码、验证（如 `npm run build`、测试命令）时由助手在环境里 **自行执行**；**不要**以「请你本地点运行 / 请在终端执行」作为主要交付方式（用户明确要求自己跑时除外）。
- **协作流程**：改代码后 **立即** `git add` → `commit` → `push`，再回复用户。
